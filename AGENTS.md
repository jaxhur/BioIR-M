# 项目 Codex 规则

## 编码规范

编写、修改、重构、调试、审查、测试或生成代码前，必须先使用 `coding-standards` 技能并遵循其要求。

默认保留已有用户注释。除非注释错误、过时、重复，或其关联行为被有意删除，否则不得删除或改写。新代码应沿用项目周边的注释风格，为类、方法、参数、返回值和关键的非显然逻辑补充有用说明。

修改代码后，必须向用户说明：修改了哪些文件或区域、修改内容和实现方式，以及运行了哪些验证；若未运行验证，必须说明原因。

## Git 自动提交

每个独立且可验证的用户请求完成后：

1. 运行与本次改动相关的验证。
2. 仅暂存本次任务修改的文件，不得使用 `git add -A` 或提交无关改动。
3. 创建一个语义清晰的 Git 提交，使用 Conventional Commits 格式，例如：
   - `feat: add DRR routing module`
   - `fix: handle non-square token grids`
   - `docs: update experiment plan`
4. 工作区存在与本任务无关的修改时，不得自动提交；应向用户说明并请求决定。
5. 验证失败时不得提交，除非用户明确要求提交当前进度。

不要在每一次文件保存或几行代码的中间状态创建提交；以一个完成并验证过的需求或小功能为一个提交单元，以保证历史可读且方便回溯。
